const Loading = () => {
  return (
    <div className="w-screen h-screen max-h-screen flex justify-center items-center bg-white fixed top-0 left-0">
      <p className="animate-pulse text-primary">Loading...</p>
    </div>
  );
};

export default Loading;
