const Spinner = () => {
  return (
    <span
      className="animate-spin-fast inline-block h-10 w-10 mr-3 rounded-full border-4 border-primary"
      style={{ borderTopColor: 'transparent' }}
    ></span>
  );
};

export default Spinner;
